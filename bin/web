#!/usr/bin/env sh
# In dev mode, Finatra uses the local_docroot flag to load resources.
# Override the default with zipkin-web prefix

./gradlew zipkin-web:installDist
./zipkin-web/build/install/zipkin-web/bin/zipkin-web "$@"
