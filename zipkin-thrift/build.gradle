buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath "com.yodle.griddle:griddle:${commonVersions.griddle}"
    }
}

apply plugin: 'idl'
apply plugin: 'thrift'

dependencies {
    compile "org.apache.thrift:libthrift:${commonVersions.thrift}"
}
